{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install": "truffle compile && cd frontend && npm install",
    "start": "cd frontend && npm run serve",
    "build": "cd frontend && npm build",
    "lint": "vue-cli-service lint",
    "test": "truffle test"
  },
  "dependencies": {
    "truffle": "^5.0.1",
    "truffle-assertions": "^0.8.0"
  }
}
